--[[     JarHub Ultimate v6.0     - Botón flotante para abrir/cerrar     - Pestañas (tabs) para organizar funciones     - Persistente al morir (no se pierde el menú)     - Invisibilidad funcional     - Ancho moderado (350px)     - Móvil y arrastrable --]]  -- Servicios local Players = game:GetService("Players") local RunService = game:GetService("RunService") local UserInputService = game:GetService("UserInputService") local LP = Players.LocalPlayer local Camera = workspace.CurrentCamera local CoreGui = game:GetService("CoreGui") local ReplicatedStorage = game:GetService("ReplicatedStorage") local Workspace = game:GetService("Workspace")  -- Variables persistentes (sobreviven a muerte) getgenv().JarHub = getgenv().JarHub or {} local cfg = getgenv().JarHub if not cfg.loaded then     cfg.loaded = true     -- Player Cheats     cfg.aimbot = false     cfg.silent = false     cfg.trigger = false     cfg.autoshot = false     cfg.magic = false     cfg.speed = false     cfg.speedVal = 50     cfg.infjump = false     cfg.noclip = false     cfg.fly = false     cfg.flySpeed = 50     cfg.invisible = false     cfg.jumpPow = false     cfg.jumpVal = 80     cfg.god = false     -- ESP & Hitbox     cfg.esp = false     cfg.hitbox = false     cfg.hitboxSize = 10     -- External Scripts     cfg.killAll = false     cfg.autoTP = false     cfg.autoFarm = false     cfg.autoBuy = false     cfg.antiAFK = false     -- Events & Boxes     cfg.autoBuyEvent = false     -- Auto Teleport     cfg.tpZone = false     -- UI     cfg.menuOpen = true end  -- ========== ANTI-KICK ========== local oldKick = LP.Kick LP.Kick = function(self, msg)     if msg and msg:find("BAC-6237") then return end     return oldKick(self, msg) end local oldTeleport = game.TeleportService.Teleport game.TeleportService.Teleport = function() end  -- ========== UTILIDAD ========== local function getClosest()     local closest, dist = nil, math.huge     local center = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)     for _, plr in pairs(Players:GetPlayers()) do         if plr ~= LP and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then             local pos, on = Camera:WorldToViewportPoint(plr.Character.HumanoidRootPart.Position)             if on then                 local d = (Vector2.new(pos.X, pos.Y) - center).Magnitude                 if d < dist then dist, closest = d, plr end             end         end     end     return closest end  -- ========== HOOK DE REMOTES (SILENT AIM) ========== local origFire local function hookRemote(remote)     if not origFire and remote and remote.FireServer then         origFire = remote.FireServer         remote.FireServer = function(self, ...)             if cfg.silent or cfg.magic then                 local target = getClosest()                 if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then                     local args = {...}                     args[1] = target.Character.HumanoidRootPart.Position                     return origFire(self, unpack(args))                 end             end             return origFire(self, ...)         end     end end LP.CharacterAdded:Connect(function(char)     task.wait(0.5)     local tool = char:FindFirstChildOfClass("Tool")     if tool then         local remote = tool:FindFirstChild("RemoteEvent") or tool:FindFirstChild("ShootEvent")         if remote then hookRemote(remote) end     end end)  -- ========== TRIGGERBOT / AUTOSHOT ========== local mouse = LP:GetMouse() RunService.RenderStepped:Connect(function()     if cfg.autoshot and getClosest() then         mouse:Button1Click()         task.wait(0.1)     end     if cfg.trigger then         local target = getClosest()         if target and target.Character then             local pos, on = Camera:WorldToViewportPoint(target.Character.HumanoidRootPart.Position)             if on and (Vector2.new(pos.X, pos.Y) - Vector2.new(mouse.X, mouse.Y)).Magnitude < 50 then                 mouse:Button1Click()             end         end     end end)  -- ========== SPEED HACK ========== task.spawn(function()     while true do         if cfg.speed and LP.Character and LP.Character:FindFirstChild("Humanoid") then             LP.Character.Humanoid.WalkSpeed = cfg.speedVal         elseif LP.Character and LP.Character:FindFirstChild("Humanoid") and LP.Character.Humanoid.WalkSpeed ~= 16 then             LP.Character.Humanoid.WalkSpeed = 16         end         task.wait(0.1)     end end)  -- ========== INFINITE JUMP ========== UserInputService.JumpRequest:Connect(function()     if cfg.infjump and LP.Character and LP.Character:FindFirstChild("Humanoid") then         LP.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)     end end)  -- ========== NOCLIP ========== RunService.Stepped:Connect(function()     if cfg.noclip and LP.Character then         for _, part in ipairs(LP.Character:GetDescendants()) do             if part:IsA("BasePart") then part.CanCollide = false end         end     end end)  -- ========== FLY ========== local flying, bv, bg = false local function startFly()     flying = true     local char = LP.Character     if not char then return end     local hum = char:FindFirstChild("Humanoid")     if hum then hum.PlatformStand = true end     bv = Instance.new("BodyVelocity")     bv.MaxForce = Vector3.new(1,1,1)*10000     bv.Parent = char.HumanoidRootPart     bg = Instance.new("BodyGyro")     bg.MaxTorque = Vector3.new(1,1,1)*10000     bg.Parent = char.HumanoidRootPart     while flying and cfg.fly do         local move = Vector3.new()         if UserInputService:IsKeyDown(Enum.KeyCode.W) then move = move + char.HumanoidRootPart.CFrame.LookVector end         if UserInputService:IsKeyDown(Enum.KeyCode.S) then move = move - char.HumanoidRootPart.CFrame.LookVector end         if UserInputService:IsKeyDown(Enum.KeyCode.A) then move = move - char.HumanoidRootPart.CFrame.RightVector end         if UserInputService:IsKeyDown(Enum.KeyCode.D) then move = move + char.HumanoidRootPart.CFrame.RightVector end         if UserInputService:IsKeyDown(Enum.KeyCode.Space) then move = move + Vector3.new(0,1,0) end         if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then move = move - Vector3.new(0,1,0) end         bv.Velocity = move * cfg.flySpeed         bg.CFrame = Camera.CFrame         task.wait()     end     if bv then bv:Destroy() end     if bg then bg:Destroy() end     if hum then hum.PlatformStand = false end end task.spawn(function()     while true do         if cfg.fly and not flying then coroutine.wrap(startFly)() end         if not cfg.fly and flying then flying = false end         task.wait(0.1)     end end)  -- ========== INVISIBLE (GHOST MODE) ========== local function setInvisible(state)     if LP.Character then         for _, part in ipairs(LP.Character:GetDescendants()) do             if part:IsA("BasePart") then                 part.Transparency = state and 1 or 0             end         end     end end RunService.RenderStepped:Connect(function()     if cfg.invisible then setInvisible(true) end end) LP.CharacterAdded:Connect(function()     if cfg.invisible then setInvisible(true) end end)  -- ========== GOD MODE ========== RunService.Stepped:Connect(function()     if cfg.god and LP.Character and LP.Character:FindFirstChild("Humanoid") then         LP.Character.Humanoid.BreakJointsOnDeath = false         LP.Character.Humanoid.Health = math.huge     end end)  -- ========== ESP ========== local espStorage = Instance.new("Folder") espStorage.Name = "JarHub_ESP_" .. tostring(math.random(1000,9999)) espStorage.Parent = CoreGui local espBoxes = {} local function addESP(player)     if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then return end     local box = Instance.new("BoxHandleAdornment")     box.Size = Vector3.new(4,5,2)     box.Adornee = player.Character.HumanoidRootPart     box.Color3 = Color3.fromRGB(255,0,0)     box.Transparency = 0.5     box.AlwaysOnTop = true     box.Parent = espStorage     espBoxes[player] = box end local function clearESP()     for _, box in pairs(espBoxes) do if box then box:Destroy() end end     espBoxes = {} end RunService.RenderStepped:Connect(function()     if cfg.esp then         for _, plr in pairs(Players:GetPlayers()) do             if plr ~= LP and not espBoxes[plr] and plr.Character then addESP(plr) end         end     else         clearESP()     end end)  -- ========== HITBOX EXPANDER ========== RunService.Stepped:Connect(function()     if cfg.hitbox then         for _, plr in pairs(Players:GetPlayers()) do             if plr ~= LP and plr.Character then                 local head = plr.Character:FindFirstChild("Head")                 if head then head.Size = Vector3.new(cfg.hitboxSize, cfg.hitboxSize, cfg.hitboxSize) end             end         end     end end)  -- ========== KILL ALL ========== local function killAllEnemies()     for _, plr in pairs(Players:GetPlayers()) do         if plr ~= LP and plr.Character and plr.Character:FindFirstChild("Humanoid") then             pcall(function() plr.Character.Humanoid.Health = 0 end)         end     end     for _, rem in ipairs(ReplicatedStorage:GetDescendants()) do         if rem:IsA("RemoteEvent") then             pcall(function() rem:FireServer("KillAll") end)             pcall(function() rem:FireServer("all") end)         end     end end task.spawn(function()     while true do         if cfg.killAll then killAllEnemies() end         task.wait(1)     end end)  -- ========== AUTO TELEPORT ========== task.spawn(function()     while true do         if cfg.autoTP then             local target = getClosest()             if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") and LP.Character then                 LP.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame * CFrame.new(0,0,-3)             end         end         task.wait(0.5)     end end)  -- ========== AUTO FARM ========== task.spawn(function()     while true do         if cfg.autoFarm then             local item = Workspace:FindFirstChild("Coin") or Workspace:FindFirstChild("Gem")             if item and item:IsA("BasePart") and LP.Character then                 LP.Character.HumanoidRootPart.CFrame = item.CFrame             end         end         task.wait(1)     end end)  -- ========== AUTO BUY ========== task.spawn(function()     while true do         if cfg.autoBuy then             local buy = ReplicatedStorage:FindFirstChild("BuyBox")             if buy then pcall(function() buy:FireServer("MythicBox") end) end         end         task.wait(5)     end end)  -- ========== ANTI-AFK ========== local lastMove = tick() UserInputService.InputBegan:Connect(function() lastMove = tick() end) task.spawn(function()     while true do         if cfg.antiAFK and tick() - lastMove > 30 then             mouse.Move(Vector2.new(mouse.X + 1, mouse.Y))             lastMove = tick()         end         task.wait(30)     end end)  -- ========== AUTO BUY EVENT ========== task.spawn(function()     while true do         if cfg.autoBuyEvent then             local event = ReplicatedStorage:FindFirstChild("EventBuy")             if event then pcall(function() event:FireServer() end) end         end         task.wait(10)     end end)  -- ========== TELEPORT TO ZONE ========== task.spawn(function()     while true do         if cfg.tpZone then             local zone = Workspace:FindFirstChild("Center")             if zone and LP.Character then                 LP.Character.HumanoidRootPart.CFrame = zone.CFrame             end         end         task.wait(1)     end end)  -- ========== INTERFAZ CON PESTAÑAS (TABS) Y BOTÓN FLOTANTE ========== local gui = Instance.new("ScreenGui") gui.Name = "JarHub_GUI_" .. tostring(math.random(1000,9999)) gui.ResetOnSpawn = false gui.Parent = CoreGui  -- Botón flotante (abrir/cerrar) local toggleBtn = Instance.new("ImageButton") toggleBtn.Size = UDim2.new(0, 50, 0, 50) toggleBtn.Position = UDim2.new(0, 10, 0, 10) toggleBtn.BackgroundColor3 = Color3.fromRGB(0,0,0) toggleBtn.BackgroundTransparency = 0.6 toggleBtn.Image = "rbxasset://textures/ui/Arrow.png" toggleBtn.ImageColor3 = Color3.fromRGB(255,255,255) toggleBtn.Parent = gui  -- Menú principal local menu = Instance.new("Frame") menu.Size = UDim2.new(0, 350, 0, 450) menu.Position = UDim2.new(0, 70, 0, 10) menu.BackgroundColor3 = Color3.fromRGB(30,30,35) menu.BackgroundTransparency = 0.15 menu.BorderSizePixel = 0 menu.Visible = cfg.menuOpen menu.Parent = gui  -- Barra de título (para mover) local titleBar = Instance.new("Frame") titleBar.Size = UDim2.new(1, 0, 0, 30) titleBar.BackgroundColor3 = Color3.fromRGB(45,45,55) titleBar.Parent = menu local title = Instance.new("TextLabel") title.Size = UDim2.new(1, 0, 1, 0) title.Text = "JarHub Ultimate v6.0" title.TextColor3 = Color3.fromRGB(255,255,255) title.BackgroundTransparency = 1 title.Parent = titleBar  -- Arrastrar menú local dragging = false local dragStart, startPos titleBar.InputBegan:Connect(function(i)     if i.UserInputType == Enum.UserInputType.MouseButton1 then         dragging = true         dragStart = i.Position         startPos = menu.Position     end end) UserInputService.InputChanged:Connect(function(i)     if dragging and i.UserInputType == Enum.UserInputType.MouseMovement then         local delta = i.Position - dragStart         menu.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)     end end) UserInputService.InputEnded:Connect(function(i)     if i.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end)  -- Botón flotante: toggle menú toggleBtn.MouseButton1Click:Connect(function()     cfg.menuOpen = not cfg.menuOpen     menu.Visible = cfg.menuOpen end)  -- Pestañas (tabs) local tabs = {"Player Cheats", "ESP & Hitbox", "Ext. Scripts", "Events & Boxes", "Auto TP"} local tabBtns = {} local tabContents = {}  for i, name in ipairs(tabs) do     local btn = Instance.new("TextButton")     btn.Size = UDim2.new(0, 62, 0, 25)     btn.Position = UDim2.new(0, 5 + (i-1)*68, 0, 35)     btn.Text = name     btn.BackgroundColor3 = Color3.fromRGB(55,55,65)     btn.TextColor3 = Color3.fromRGB(255,255,255)     btn.TextSize = 11     btn.Parent = menu     tabBtns[i] = btn          local content = Instance.new("ScrollingFrame")     content.Size = UDim2.new(1, -10, 1, -70)     content.Position = UDim2.new(0, 5, 0, 
